# Miro Codex Plugins

Install the marketplace:

```bash
codex plugin marketplace add miro/codex-plugin-marketplace --ref main
```

Install Backend Code Guide:

```bash
codex plugin add backend-code-guide@miro-codex-plugins
```

Refresh updates:

```bash
codex plugin marketplace upgrade miro-codex-plugins
```
